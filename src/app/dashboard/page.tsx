import Dashboard from "@/componants/Dashboard";

export const metadata = {
  title: "Dashboard | Danu Creations",
  description: "Monitor the progress of your photography projects from booking to delivery.",
};

export default function DashboardPage() {
  return <Dashboard />;
}
